# GAP层

# 开发工作

HCI event 分发器。

GAP event 定义和分发。

ACL 连接基础流程处理。
    Feature exchange
    version exchange
    MTU exchange(if supported)
    database hash read(if supported)
    security request(if supported)
    connection parameter update request(7.5ms)

BLE角色接口抽象：
legacy adv
extend adv

acl peripheral

cis peripheral

legacy scan
extend scan

acl central

cis central
...


